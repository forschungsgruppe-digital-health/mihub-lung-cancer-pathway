# CLAUDE.md

@AGENTS.md

> All project context, the quality gate, conventions, branching and hard rules live
> in the `AGENTS.md` imported above (tool-agnostic, canonical — also read by Codex,
> Cursor, Gemini and other agents). This file exists only so Claude Code picks up the
> same single source. Add Claude-Code-specific notes below if ever needed.
>
> Skills live in `skills/` and are exposed to Claude Code via the `.claude/skills`
> symlink (→ `../skills`): currently `bpmn-conformance`.
